import numpy


class LinearRegression(object):

    def __init__(self, x_train, y_train, lambda_value):
        pass

    def predict(self, x_test):
        predictions = list()

        return predictions
